name: Build APK from ZIP

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Unzip and Organize
        run: |
          unzip LiveStreamApp.zip
          find . -maxdepth 2 -name "build.gradle*" -execdir cp -r . ../ \; || true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Fix AndroidX and Namespace
        run: |
          # تفعيل AndroidX في ملف gradle.properties
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties
          
          # إضافة الـ Namespace تلقائياً بناءً على ملف المانيفست
          PACKAGE_NAME=$(grep -oP 'package="\K[^"]+' app/src/main/AndroidManifest.xml)
          sed -i "/android {/a \    namespace '$PACKAGE_NAME'" app/build.gradle || true

      - name: Build with Installed Gradle
        run: gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LiveStream-App
          path: "**/build/outputs/apk/debug/*.apk"
